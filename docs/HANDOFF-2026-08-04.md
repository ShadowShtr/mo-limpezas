# Handoff — 2026-08-04

Documento para retomar este trabalho noutro computador. Lê isto primeiro,
depois `docs/ESTADO-ATUAL.md`.

## Repositório e branches

- Repositório: `https://github.com/ShadowShtr/mo-limpezas`
- Branch de trabalho: `fix/atomic-contract-calendar-sync`
- **SHA exato do último commit desta branch**: ver `git log -1` (esta
  secção fica desatualizada rápido; o número exato importa menos do que
  os factos abaixo, que continuam válidos independentemente do commit).
- Commit-base do `master` no início desta sessão: `5581784`
- **`master` atual**: `0e6fdcba720118f3afbcff12f20fd53b99135ab6` — **PR #26 FOI
  MESCLADA** por `ShadowShtr` em `2026-08-04T18:30:32Z` (confirmado via API do
  GitHub, não fui eu que mesclei). Continha exatamente os 3 ficheiros previstos
  (064, 065, errata). **Isto disparou um deploy de PRODUÇÃO automático
  na Vercel** (confirmado — deployment `target: production`, aliases
  `molimpezas.pt`/`www.molimpezas.pt`, criado ~18:30 UTC, coincide com o
  merge ao segundo). Como a PR só trazia migrations já aplicadas + uma
  errata documental, não deve ter mudado comportamento da app — mas isto
  é inferência, não uma garantia direta.
- **PR #27** (`fix/gitattributes-migration-checksums` → `master`) —
  **fechada como substituída, não mesclada**:
  https://github.com/ShadowShtr/mo-limpezas/pull/27 — assumia que o ledger
  tinha checksums CRLF para `001`–`063` e só `064`/`065` em LF. Um `--dry-run`
  num `git worktree` limpo (não o working directory histórico desta máquina)
  refutou isso: 46 das 63 migrations antigas esperam LF, sem padrão por
  intervalo. Mapeamento completo em
  `docs/atomicidade-audit/migration-checksum-map-2026-08-05.md`.
- **PR #28** (`fix/migration-checksum-portability` → `master`) — **MESCLADA**,
  merge commit `f54469fc2f95b21da6e3a5dad79b72d5e88c9696`, confirmado
  `mergedAt: 2026-08-05T09:14:20Z`, deploy Vercel `state: success`
  ("Deployment has completed"). Substitui a PR #27: `scripts/lib/migration-checksum.mjs`
  aceita checksum histórico em RAW/LF/CRLF (elimina a dependência de
  `core.autocrlf`/SO do checkout); migrações novas gravam sempre em LF
  normalizado; `022_storage_bucket_collaborator_documents.sql` (única
  migration cujo checksum não bate com nenhuma versão em git — investigado,
  não corrigido às cegas, produção correta via `023`) formalizada como
  exceção estreita em `supabase/migration-policy.json` →
  `knownChecksumExceptions`, com aviso explícito (`⚠ CHECKSUM EXCEPTION
  ACEITE`), não silencioso. Validado pós-merge em `master`: `git pull
  --ff-only` limpo (fast-forward `aeab514..f54469f`), `--dry-run`:
  ```
  ⚠ CHECKSUM EXCEPTION ACEITE:
     022_storage_bucket_collaborator_documents.sql
     Motivo: divergência histórica documentada; estado final garantido pela 023.
  ✅ Nenhuma migração pendente.
  🎉 Concluído.
  ```
  Nenhuma migration foi executada (dry-run), nenhuma divergência desconhecida
  apareceu. `066`/`067` não entraram nesta PR (não existem em `master`) — ficam
  para decisão separada, ver "Ordem exata dos próximos passos" abaixo.
- **Diagnóstico de isolamento multiempresa (2026-08-05, read-only, só
  `SELECT`)**: encontrados 2 bloqueadores críticos em produção, **B.
  BLOQUEADOR** — nenhuma empresa/conta de teste foi criada.
  1. `handle_new_user()` (trigger `on_auth_user_created`) lia `company_id`
     e `role` de `raw_user_meta_data` — preenchível por qualquer chamador
     do signup público do Supabase Auth (anon key, pública por desenho).
     Permitia criar profile `role='admin'` em qualquer `company_id`,
     incluindo a empresa real, sem autorização.
  2. Policy `profiles_update_own` (`FOR UPDATE USING (id = auth.uid())`,
     desde `014`) sem `WITH CHECK` explícito — o Postgres reutiliza o
     `USING` como `CHECK`, que só valida `id`, nunca `company_id`/`role`
     da linha nova. Qualquer conta autenticada podia auto-promover-se a
     admin em qualquer empresa via `UPDATE` direto.
  Mitigação imediata (fora do SQL, confirmada pelo dono no Supabase
  Dashboard, 2026-08-05): **"Allow new users to sign up" = OFF**,
  **"Allow anonymous sign-ins" = OFF**. Login de utilizadores existentes
  não afetado.
- **PR #29** (`fix/tenant-isolation-auth-hotfix` → `master`) — **MESCLADA**
  pelo dono diretamente no GitHub, merge commit
  `0e6fdcba720118f3afbcff12f20fd53b99135ab6`, confirmado
  `mergedAt: 2026-08-05T10:29:19Z`, deploy Vercel `state: success`
  ("Deployment has completed"). Contém só `068_disable_untrusted_profile_bootstrap.sql`,
  `069_guard_profile_tenant_role.sql` e os testes estáticos
  correspondentes — nada de `066`/`067`, outbox, Realtime, contratos,
  calendário, scripts de criação de tenants, grants gerais, refatorações
  ou migrations históricas. `068` torna `handle_new_user()` neutra (só
  `RETURN NEW`, sem ler metadata nem criar profile — decisão explícita:
  não validar `companies.id` nem forçar `role='colaborador'`, porque
  isso ainda deixaria um estranho auto-inscrever-se numa empresa real
  cujo UUID conheça). `069` cria um trigger `BEFORE UPDATE` em
  `public.profiles` que bloqueia mudança de `company_id` fora de
  contexto `service_role` (mesmo para admin/gestor) e bloqueia mudança
  de `role` fora de `service_role` exceto para admin/gestor da mesma
  empresa com empresa inalterada; recria `profiles_update_own` com
  `WITH CHECK` explícito como reforço. Validado pós-merge em `master`:
  `git pull --ff-only` limpo (fast-forward `f54469f..0e6fdcb`),
  `--dry-run`:
  ```
  ⚠ CHECKSUM EXCEPTION ACEITE:
     022_storage_bucket_collaborator_documents.sql
     Motivo: divergência histórica documentada; estado final garantido pela 023.
  (dry-run) aplicaria: 068_disable_untrusted_profile_bootstrap.sql
  (dry-run) aplicaria: 069_guard_profile_tenant_role.sql
  🎉 Concluído.
  ```
  Aplicação de `068`/`069` exige autorização separada, uma migration de
  cada vez, com validação entre elas.
- **`068` aplicada em produção em 2026-08-05T10:45:37.569Z**, isolada de
  `069` via `git worktree` temporário (069 movida para fora de
  `supabase/migrations/` só nesse worktree, nunca commitada/enviada,
  restaurada depois; worktree principal e branch nunca tocados). Checksum
  registado no ledger: `5a92fac23b2b2c4bfa15ca34e79c175331ba2f0b3ea7c1d5bb26330ade28a14e`
  (LF normalizado, calculado pelo módulo oficial do runner) — bate
  exatamente com o previsto no preflight. Validado por leitura pós-
  aplicação: `handle_new_user()` contém só `RETURN NEW` (sem
  `raw_user_meta_data`, sem `INSERT INTO public.profiles`); trigger
  `on_auth_user_created` continua a existir e a apontar para
  `handle_new_user`; `068` registada uma única vez no ledger; `069`
  ausente do ledger. `--dry-run` completo no `master` (com `069` de
  volta no ficheiro):
  ```
  ⚠ CHECKSUM EXCEPTION ACEITE:
     022_storage_bucket_collaborator_documents.sql
     Motivo: divergência histórica documentada; estado final garantido pela 023.
  (dry-run) aplicaria: 069_guard_profile_tenant_role.sql
  🎉 Concluído.
  ```
  `068` não voltou a aparecer como pendente, nenhuma divergência de
  checksum. Contagens agregadas antes/depois inalteradas
  (`companies: 1`, `profiles: 30`) — nenhuma conta ou empresa foi criada.
  **`069` continua pendente, não aplicada — a vulnerabilidade da policy
  `profiles_update_own` (bloqueador 2, auto-promoção via `UPDATE`
  direto) continua ativa em produção até `069` ser aplicada.** Rollback
  de `068` documentado na PR #29 (restaura a definição antiga de
  `handle_new_user()` — reintroduziria o bloqueador 1; só usar em
  emergência com falha funcional comprovada).

## Commits desta fase (mais recente primeiro)

```text
8b52f6b feat(migration 066): protege public._migrations; renumera fundação do outbox para 067
41dab02 fix(migration 066): record_company_change_event adquire lock antes de verificar idempotência
e72439f docs: portão de autorização da 066 — reversão testada, incidente investigado, achado maior de grants
3b7c0cf feat(migration 066): fundação do outbox — sequência por empresa, domain_mutations idempotente, eventos imutáveis
ae1322f docs: regista aplicação real da migration 065 em produção
7e93e80 feat(migration 065): revoga privilégios de anon/authenticated (incl. TRUNCATE) em domain_mutations/company_change_events
fee2a88 fix(T03): corrige contagem errada de "34 triggers de revision"
fe62685 docs: fecha pendência do T03 — confirmado pelo dono que todos os projetos Supabase são Free
4771329 docs: regista aplicação real da migration 064 em produção
a67baaf feat(migration 064): revoga grants públicos de 3 RPCs SECURITY DEFINER, ensaiada com BEGIN...ROLLBACK
fe1d612 chore(T03): fingerprint read-only do Supabase existente + corrige lacuna no backup
58e418b chore(consistencia): consolida documentos e bloqueia migrations congeladas
6e19d29 feat(T01): prova inequívoca de ambiente/versão em /api/health, /api/health/deep e página de diagnóstico
0b65aa2 docs(atomicidade): regista investigação 064/065, auditoria de schema e revisão do motor de recorrência
13cf63d fix(recorrencia): motor canónico único; corrige contrato mensal a só gerar 1 ocorrência em janelas de vários meses
ffde844 feat(atomicidade): clientes/faturas passam a exigir revisão e RPC; contratos propagam erro em vez de engolir
6de5fe7 chore(migrations): adiciona 064/065 (atomicidade/outbox) como artefactos locais, não aplicadas
```

## Migrations — o que está aplicado, o que só existe no Git

| Migration | Aplicada em produção? | No Git (esta branch)? | No `master`? |
| --- | --- | --- | --- |
| 001–063 + 4 legadas | ✅ Sim | ✅ Sim | ✅ Sim |
| 064 (revoke grants funções) | ✅ Sim (2026-08-04 16:33 UTC) | ✅ Sim | ❌ Não — PR #26 aberta |
| 065 (revoke grants outbox tables) | ✅ Sim (2026-08-04 16:47 UTC) | ✅ Sim | ❌ Não — PR #26 aberta |
| **066 (protege `_migrations`)** | ❌ **NÃO APLICADA** | ✅ Sim | ❌ Não |
| **067 (fundação do outbox)** | ❌ **NÃO APLICADA** | ✅ Sim | ❌ Não |

**066 e 067 estão explicitamente bloqueadas para produção** — ver secção
"Bloqueios" abaixo.

## Testes e resultados desta fase

```text
npm test: 33 ficheiros, 554 testes, 0 falhas
npx tsc --noEmit: limpo
npm run lint: 0 erros/avisos
npm run build: limpo, ~50 páginas
node scripts/rehearse-066-secure-migrations-ledger.mjs: 14/14 (BEGIN...ROLLBACK contra produção)
node scripts/rehearse-067-outbox-foundation.mjs: 21/21 (BEGIN...ROLLBACK contra produção, incl. teste empírico de isolamento por empresa)
Rollback de 066 e 067: testados (aplicar → rollback → fingerprint idêntico ao original)
```

## Correções realizadas nesta fase

1. **Motor canónico de recorrência** (`src/domain/scheduling/recurrence-engine.ts`) —
   corrige contratos mensais a só gerarem 1 ocorrência em janelas de
   vários meses.
2. **Atomicidade clientes/faturas** — `archiveCliente`/`deleteCliente`/
   `updateInvoiceStatus` exigem `expected_revision` + `mutation_id`,
   chamam RPCs em vez de escrita direta.
3. **`/api/health`, `/api/health/deep`, `/dashboard/sistema/diagnostico`** —
   prova de ambiente/commit/migration sem expor segredos.
4. **Migration 064** (aplicada): revoga `EXECUTE` de `anon`/`authenticated`/
   `PUBLIC` em 3 RPCs `SECURITY DEFINER`.
5. **Migration 065** (aplicada): fecha `TRUNCATE` aberto em
   `domain_mutations`/`company_change_events`.
6. **Migration 066** (não aplicada): fecha `_migrations` — RLS desligada +
   grants completos, único achado com exploração prática confirmada
   (PostgREST expõe INSERT/UPDATE/DELETE, ao contrário do TRUNCATE).
7. **Migration 067** (não aplicada, renumerada de 066): fundação do
   outbox — `company_sync_state`, `domain_mutations` idempotente,
   `company_change_events` imutável, com correção de uma corrida real em
   `record_company_change_event` encontrada em revisão externa.
8. **Errata da 065** sem editar o ficheiro aplicado —
   `docs/atomicidade-audit/065-errata-explorabilidade-truncate.md`.

## Vulnerabilidades encontradas

- **Fechada (064)**: 3 RPCs `SECURITY DEFINER` executáveis por
  `anon`/`authenticated`/`PUBLIC`.
- **Fechada (065)**: `TRUNCATE`/`INSERT`/`UPDATE`/`DELETE` abertos em
  `domain_mutations`/`company_change_events` para `anon`/`authenticated`.
  Investigado: sem evidência de exploração passada
  (`docs/atomicidade-audit/incidente-truncate-2026-08-04.md`).
- **Corrigida no SQL, não aplicada (066)**: `_migrations` com RLS
  desligada + grants completos — exploração prática confirmada (via
  PostgREST, não só TRUNCATE).
- **Identificada, não corrigida — 528 grants classificados**: 44
  tabelas/views com `TRUNCATE`/`TRIGGER`/`REFERENCES`; 6 views com
  escrita direta sem `security_invoker` confirmado (precisa investigação
  própria); resto precisa auditoria tabela a tabela. Ver
  `docs/atomicidade-audit/incidente-truncate-2026-08-04.md`, secção de
  classificação.

## Estado do motor de recorrência

Concluído e testado (32 testes próprios +12 antigos, todos a passar).
`getOccurrences` (wrapper) e o cron `generate-services` herdam a correção
automaticamente, sem alteração de código.

## Estado da auditoria de grants (528)

Classificados, não corrigidos (exceto `_migrations`, migration 066). Ver
`docs/atomicidade-audit/incidente-truncate-2026-08-04.md` para a matriz
completa de categorias e o que falta investigar por view.

## Bloqueio do staging

Preciso de um projeto Supabase de staging/descartável para:

- teste de concorrência real (2+ ligações simultâneas reais — dentro de
  uma transação de ensaio `BEGIN...ROLLBACK`, uma segunda ligação não vê
  DDL não confirmado, por isso isto não pode ser testado contra produção);
- teste de Realtime real (2 clientes autenticados, isolamento, reconexão,
  duplicação/perda de eventos).

**Não consigo criar um projeto Supabase sozinho** — sem acesso ao painel
de gestão da conta. O dono tem de criar o projeto e guardar as
credenciais só localmente (`.env.staging.local`, nunca no Git).

## Variáveis de ambiente necessárias (nomes, nunca valores)

Já existentes (produção, `.env.local` local, nunca commitado):

```text
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
SUPABASE_DB_URL
NEXT_PUBLIC_MAPBOX_TOKEN
NEXT_PUBLIC_VAPID_PUBLIC_KEY
VAPID_PRIVATE_KEY
GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET
NEXT_PUBLIC_APP_URL
AUDIT_APP_URL
CRON_SECRET
RESEND_API_KEY
RESEND_FROM_EMAIL
COMPANY_PHONE
```

Novas, para staging (ficheiro local `.env.staging.local`, nunca no Git):

```text
TEST_DATABASE_URL           (migrations + concorrência entre 2 ligações)
STAGING_SUPABASE_URL
STAGING_SUPABASE_PUBLISHABLE_KEY   (login dos 2 utilizadores + Realtime)
STAGING_SUPABASE_SECRET_KEY        (só num script servidor local, criar/apagar utilizadores sintéticos)
```

## Descoberta pós-merge (revisão externa, depois do merge da PR #26)

Pedido: correr `git switch master && git pull --ff-only && node scripts/run-migrations.mjs --dry-run` e confirmar 064/065 reconhecidas, sem checksum divergente. Resultado real, com dois achados distintos:

1. **Falso positivo, causado por mim**: o `.gitattributes` desta branch
   (`*.sql text eol=lf` global) nunca tinha ido para `master` — quando
   testado lá, revelou que os checksums das **21 migrations antigas**
   (014-043 + 2 datadas) no ledger de produção foram todos calculados
   sobre **CRLF** (o checkout normal desta máquina, sem
   `.gitattributes`), não LF. Só `064`/`065` (escritas e aplicadas na
   mesma sessão, sem nunca passar por um checkout) têm baseline LF. Uma
   regra global partia as 21 antigas para "corrigir" só 2. Corrigido
   nesta branch e numa PR própria para `master` (**PR #27**, não
   mesclada): regra só para os 2 ficheiros que precisam de LF.
2. **Achado real, pré-existente, não relacionado com esta sessão**:
   `022_storage_bucket_collaborator_documents.sql` continua a aparecer
   como divergente no `--dry-run` — foi editado 2× depois de aplicado
   (commits `d0febb5`/`8f924b4`, muito antes desta sessão) e o checksum
   do ledger nunca foi atualizado. Verificado por leitura direta que o
   bucket `collaborator-documents` real em produção já corresponde ao
   ficheiro atual (`public: false`, `allowed_mime_types: null`) — **sem
   deriva funcional**, só o checksum ficou desatualizado. Decisão do
   dono: documentar, não corrigir agora.

Checksums 064/065 reconfirmados depois da correção, idênticos ao ledger:
```
064: b4dfdecfe9ca19ac545a276b61c4070cecfa59a6fe65fcbfcfe4c12d68e0390b
065: 6c462ffefc5aed904cdc60d52247f7559984cc669fea15ea85199166b7ad4fed
```

**Estado do `--dry-run` no `master` agora**: falha só por causa do 022
(achado real, aceite, não urgente). Depois de mesclar a PR #27, o único
motivo de falha continua a ser o 022 — não há mais nenhum falso positivo.

## Clonagem limpa — testada de verdade, não só documentada

Confirmado nesta sessão, com um clone real numa pasta à parte (não só os
passos escritos): sem `.gitattributes`, um clone limpo desta mesma
máquina falhava 7 testes (mesma causa que já tinha aparecido ao trocar
de branch — `core.autocrlf=true` converte LF→CRLF em cada checkout, sem
aparecer no `git status`, quebrando comparações byte-a-byte entre os
rascunhos congelados de migration e `migration-policy.json`). Corrigido
com `.gitattributes` (`*.sql text eol=lf`, commit `6830e8e`). Reclonado
depois da correção: `npm ci` + `npm test` → **554/554**, sem tocar em
`.env.local` nem em nada específico da máquina. A recuperação noutro
computador está confirmada, não só assumida.

## Comandos para continuar

```powershell
git clone https://github.com/ShadowShtr/mo-limpezas.git
cd mo-limpezas
git fetch --all --tags
git switch fix/atomic-contract-calendar-sync
npm ci
# recriar .env.local manualmente (nunca vem do Git)
npm.cmd run lint
npx.cmd tsc --noEmit
npm.cmd test
npm.cmd run build
```

Depois de criar `.env.staging.local` com as 4 variáveis novas acima:

```powershell
node scripts/rehearse-066-secure-migrations-ledger.mjs
node scripts/rehearse-067-outbox-foundation.mjs
```

## Ordem exata dos próximos passos

1. ~~Decidir e mesclar (ou não) a PR #27~~ — **concluído 2026-08-05**: PR #27
   fechada como substituída (premissa refutada), PR #28 mesclada em `master`
   (`f54469f`) com a correção real. Ver secção acima.
2. ~~Decidir o que fazer com o achado do 022~~ — **concluído 2026-08-05**:
   formalizado como exceção estreita e verificável em
   `supabase/migration-policy.json` → `knownChecksumExceptions`, não como
   silêncio informal nem como reescrita do checksum histórico.
3. ~~Corrigir os 2 bloqueadores de isolamento multiempresa~~ —
   **concluído no repositório em 2026-08-05**: PR #29 mesclada
   (`0e6fdcb`), `068`/`069` no `master`. **Ainda pendente**: aplicar
   `068` e `069` à base de produção — exige autorização separada, uma
   migration de cada vez, com validação entre elas. Sem isso a
   vulnerabilidade da policy continua ativa em produção (o merge só
   colocou o SQL no repositório).
4. ~~Aplicar 068 isoladamente, validar~~ — **concluído 2026-08-05T10:45:37Z**.
   Checksum `5a92fac2...de28a14e`, validado por leitura pós-aplicação.
   Ver secção da PR #29 acima para o detalhe completo.
5. Aplicar `069` isoladamente, validar — **ainda pendente, ainda não
   autorizado**. Enquanto não for aplicada, o bloqueador 2 (auto-
   promoção via `UPDATE` direto em `profiles`) continua ativo em
   produção.
6. Só depois disso: criar empresas e contas de teste isoladas
   (`mo-testes-atomicidade`, `teste-isolamento-tenant`) e provar
   isolamento entre tenants — scripts ainda por escrever
   (`scripts/provision-test-tenants.mjs`, `test-tenant-isolation.mjs`,
   `cleanup-test-tenants.mjs`).
7. Retomar 066/067 (ainda não existem em `master` — só nesta branch
   ampla).
8. Dono cria o projeto Supabase de staging e preenche `.env.staging.local`.
9. Escrever/adaptar scripts de teste de concorrência real (2+ ligações
   simultâneas) e Realtime real contra o staging.
10. Rever resultados com o dono.
11. Investigação dedicada das 6 views com grant de escrita (fora desta
    sessão).
12. Auditoria tabela a tabela do resto dos 528 grants (fora desta sessão).
13. Confirmar no painel da Vercel (não só via CLI) que o deploy de
    produção pós-merge da PR #26 não teve nenhum efeito inesperado —
    verificação humana recomendada, mesmo sem indício de problema.

## O que continua sem autorização

- Deploy da branch ampla (`fix/atomic-contract-calendar-sync`) em produção.
- Aplicação da migration 066 em produção.
- Aplicação da migration 067 em produção.
- ~~Aplicação da migration 068 em produção~~ — feita em 2026-08-05T10:45:37Z.
- Aplicação da migration 069 em produção (está em `master`, não aplicada —
  bloqueador 2 continua ativo até ser aplicada).
- Criação de qualquer empresa ou conta de teste.
- Merge automático de qualquer branch para `master` sem apresentar
  primeiro diff/checksums/resultados.
- Correção global dos 528 grants (fora as já feitas: 064, 065, 066).
- Qualquer alteração às RPCs de negócio (`set_invoice_status_atomic`,
  `archive_client_atomic`, `delete_empty_client_atomic`,
  `delete_client_atomic`).
- Qualquer execução de rollback em produção (só ensaiado, nunca aplicado).

## Autorizado nesta fase

- Commit e push completo do trabalho para o GitHub.
- Documentação de continuidade (este ficheiro).
- Deploy de preview/staging (ambiente separado, não produção).
